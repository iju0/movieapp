import React from 'react'
import { View, Text } from 'react-native'
import ScrollContainer from '../Components/ScrollContainer'

const TVPresenter = ({loading, today}) => (
    <ScrollContainer loading={loading}>
        
    </ScrollContainer>
)

export default TVPresenter
